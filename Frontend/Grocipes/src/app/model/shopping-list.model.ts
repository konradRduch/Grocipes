export class ShoppingList{
    public id: number;
    public name: string;
    public color: number;

    constructor(id: number, name: string, color: number)
    {
        this.id = id;
        this.name = name;
        this.color = color;
    }
}